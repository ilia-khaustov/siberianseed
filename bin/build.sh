#!/bin/sh

# Creates configured and complete packages from source code.

# Tip: Run unit tests in the end of the Build task and delete failed packages.
# It will prevent exporting of buggy code to other apps.

echo "Building $root";