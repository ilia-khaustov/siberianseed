#!/bin/sh

# Runs processes, e.g. starts a HTTP server.

# Tip: Save *.pid files inside 'share' dir.
# To save pid of process execute 'echo $! > $root/share/process.pid' after running it.

echo "Starting $root";